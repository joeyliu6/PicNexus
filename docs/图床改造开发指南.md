# PicNexus 图床改造开发指南

## 📋 目录

1. [项目概述](#项目概述)
2. [阶段一：删除 TCL 图床](#阶段一删除-tcl-图床)
3. [阶段二：修改京东图床 URL](#阶段二修改京东图床-url)
4. [阶段三：添加各图床缩略图支持](#阶段三添加各图床缩略图支持)
5. [阶段四：新增美团图床](#阶段四新增美团图床)
6. [测试清单](#测试清单)

---

## 项目概述

### 改造目标

本次改造包含四个主要目标：

1. **删除 TCL 图床**：TCL 已启用防盗链，无法继续使用
2. **修改京东图床 URL**：将域名从 `dd-static.jd.com/ddimgp` 改为 `img30.360buyimg.com/imgzone`
3. **添加各图床缩略图支持**：为所有图床实现 `getThumbnailUrl` 方法
4. **新增美团图床**：添加新的图床服务（包含 Rust 后端）

### 开发顺序建议

```
阶段一：删除 TCL（最重要，避免编译错误）
  ↓
阶段二：修改京东 URL（简单，快速验证）
  ↓
阶段三：添加缩略图支持（独立功能，可分步测试）
  ↓
阶段四：新增美团图床（最复杂，需要前后端配合）
```

---

## 阶段一：删除 TCL 图床

### 为什么先删除 TCL？

- TCL 图床已启用防盗链，功能失效
- 必须先删除才能避免后续编译错误
- 删除后可以立即测试编译是否成功

### 1.1 修改类型定义（核心配置）

**文件：** `src/config/types.ts`

#### 操作 1：从 ServiceType 删除 'tcl'

**位置：** 约第 54 行

```typescript
// 修改前：
export type ServiceType = 'weibo' | 'r2' | 'tcl' | 'jd' | 'nowcoder' | 'qiyu' | 'zhihu' | 'nami';

// 修改后：
export type ServiceType = 'weibo' | 'r2' | 'jd' | 'nowcoder' | 'qiyu' | 'zhihu' | 'nami';
```

#### 操作 2：从 PUBLIC_SERVICES 删除 'tcl'

**位置：** 约第 66 行

```typescript
// 修改前：
export const PUBLIC_SERVICES: readonly ServiceType[] = ['weibo', 'tcl', 'jd', 'nowcoder', 'qiyu', 'zhihu', 'nami'] as const;

// 修改后：
export const PUBLIC_SERVICES: readonly ServiceType[] = ['weibo', 'jd', 'nowcoder', 'qiyu', 'zhihu', 'nami'] as const;
```

#### 操作 3：删除 TCLServiceConfig 接口

**位置：** 约第 118-121 行

```typescript
// 删除以下内容：
export interface TCLServiceConfig {
  enabled: boolean;
}
```

#### 操作 4：从 UserConfig 删除 tcl 字段

**位置：** 约第 141 行（services 接口内）

```typescript
// 删除：
tcl: TCLServiceConfig;
```

#### 操作 5：从 DEFAULT_CONFIG 删除 tcl 配置

**位置：** 约第 180-182 行

```typescript
// 删除：
tcl: {
  enabled: true
},
```

### 1.2 删除前端上传器目录

**操作：** 删除整个 `src/uploaders/tcl/` 目录

该目录包含以下文件（全部删除）：
- `TCLUploader.ts`
- `TCLRateLimiter.ts`
- `TCLError.ts`
- `index.ts`

### 1.3 修改上传器注册

**文件：** `src/uploaders/index.ts`

#### 操作 1：删除 TCLUploader 导入

**位置：** 约第 7 行

```typescript
// 删除：
import { TCLUploader } from './tcl/TCLUploader';
```

#### 操作 2：删除 TCL 注册代码

**位置：** 约第 26-27 行

```typescript
// 删除：
// 注册 TCL 上传器
UploaderFactory.register('tcl', () => new TCLUploader());
```

#### 操作 3：删除 TCLUploader 导出

**位置：** 约第 48 行

```typescript
// 删除：
export { TCLUploader } from './tcl';
```

### 1.4 修改多图床编排器

**文件：** `src/core/MultiServiceUploader.ts`

#### 操作 1：删除 TCL 错误转换导入

**位置：** 约第 16 行

```typescript
// 删除：
import { convertToTCLError } from '../uploaders/tcl/TCLError';
```

#### 操作 2：删除 TCL 错误转换 case

**位置：** 约第 157-159 行（switch 语句内）

```typescript
// 删除：
case 'tcl':
  structuredError = convertToTCLError(error);
  break;
```

#### 操作 3：删除 TCL 火后不管逻辑

**位置：** 约第 243-246 行

```typescript
// 删除：
// TCL：火后不管（通过 onProgress 立即返回模拟成功）
if (service === 'tcl') {
  return await this.uploadToServiceWithRetry(service, filePath, options, onProgress);
}
```

#### 操作 4：从 filterConfiguredServices 删除 TCL 判断

**位置：** 约第 375-376 行

```typescript
// 删除：
if (service === 'tcl') return true; // TCL 图床无需配置
```

### 1.5 修改 UI 组件

#### 文件 1：`src/composables/useUpload.ts`

**位置：** 约第 27 行

```typescript
// 修改前：
const serviceConfigStatus = ref({
  weibo: false,
  r2: false,
  tcl: true,    // ← 删除这行
  jd: true,
  // ...
});

// 修改后：
const serviceConfigStatus = ref({
  weibo: false,
  r2: false,
  jd: true,
  // ...
});
```

#### 文件 2：`src/components/views/UploadView.vue`

**操作 1：** 从 serviceLabels 删除 tcl（约第 67 行）

```typescript
// 删除：
tcl: 'TCL',
```

**操作 2：** 从 allServices 删除 'tcl'（约第 80 行）

```typescript
// 修改前：
const allServices: ServiceType[] = ['weibo', 'r2', 'tcl', 'jd', ...];

// 修改后：
const allServices: ServiceType[] = ['weibo', 'r2', 'jd', ...];
```

#### 文件 3：`src/uploadQueue.ts`

**位置：** 搜索 `tclLink` 并删除所有相关处理

```typescript
// 删除类似以下的代码：
tclLink?: string;
// 以及所有对 tclLink 的赋值和使用
```

#### 文件 4：`src/components/views/SettingsView.vue`

这是**最复杂的文件**，包含大量 TCL 相关代码。

**操作 1：** 从 availableServices 删除 'tcl'（约第 124 行）

```typescript
// 修改前：
const availableServices: ServiceType[] = ['weibo', 'r2', 'tcl', 'jd', ...];

// 修改后：
const availableServices: ServiceType[] = ['weibo', 'r2', 'jd', ...];
```

**操作 2：** 从 serviceLabels 删除 tcl（约第 126 行）

```typescript
// 删除：
tcl: 'TCL',
```

**操作 3：** 删除 TCL 可用性检测（约第 210-216 行）

```typescript
// 删除：
const tclAvailable = ref(false);

async function checkTclAvailability() {
  try {
    tclAvailable.value = await invoke('check_tcl_available');
  } catch (error) {
    tclAvailable.value = false;
  }
}
```

**操作 4：** 删除 TCL 冷却机制（约第 220-229 行）

```typescript
// 删除所有 TCL 冷却相关的 ref 和函数
```

**操作 5：** 从开箱即用判断删除 'tcl'（约第 770-771 行）

```typescript
// 修改前：
function isOutOfBoxService(service: ServiceType): boolean {
  return ['tcl', 'jd', 'nowcoder', 'qiyu', 'zhihu', 'nami'].includes(service);
}

// 修改后：
function isOutOfBoxService(service: ServiceType): boolean {
  return ['jd', 'nowcoder', 'qiyu', 'zhihu', 'nami'].includes(service);
}
```

**操作 6：** 删除 TCL case 处理（约第 911-912 行）

```typescript
// 删除：
case 'tcl':
  return '开箱即用';
```

**操作 7：** 删除 TCL UI 面板（约第 2200-2205 行）

搜索 `<!-- TCL 图床 -->` 并删除整个 TCL 配置面板的 HTML 代码块。

#### 文件 5：`src/components/views/history/HistoryTableView.vue`

**位置：** 约第 205 行

```typescript
// 删除：
tcl: 'TCL',
```

#### 文件 6：`src/components/views/history/HistoryLightbox.vue`

**位置：** 约第 47 行

```typescript
// 删除：
tcl: 'TCL',
```

#### 文件 7：`src/components/views/HistoryView.vue`

**位置：** 约第 47 行

```typescript
// 删除：
{ label: 'TCL', value: 'tcl' }
```

#### 文件 8：`src/components/views/LinkCheckerView.vue`

**位置：** 约第 60 行

```typescript
// 删除：
{ label: 'TCL 图床', value: 'tcl' },
```

#### 文件 9：`src/services/RetryService.ts`

搜索 `tcl: 'TCL'` 并删除所有出现（约第 105、221、387 行）

```typescript
// 删除：
tcl: 'TCL',
```

#### 文件 10：`src/components/UploadQueue.vue`

**操作 1：** 从 serviceNames 删除（约第 122 行）

```typescript
// 删除：
tcl: 'TCL',
```

**操作 2：** 从 serviceIcons 删除（约第 139 行）

```typescript
// 删除 tcl 相关的 icon 配置
```

#### 文件 11：`src/uploaders/base/IUploader.ts`

**位置：** 约第 27 行

```typescript
// 修改注释示例，删除 'tcl'
// 修改前：
// 支持的服务类型：'weibo' | 'r2' | 'tcl' | 'jd' | ...

// 修改后：
// 支持的服务类型：'weibo' | 'r2' | 'jd' | ...
```

### 1.6 删除 Rust 后端

#### 操作 1：删除 tcl.rs 文件

**文件：** `src-tauri/src/commands/tcl.rs`

直接删除该文件。

#### 操作 2：修改 mod.rs

**文件：** `src-tauri/src/commands/mod.rs`

```rust
// 删除：
pub mod tcl;
```

#### 操作 3：修改 main.rs

**文件：** `src-tauri/src/main.rs`

搜索并删除以下命令注册：

```rust
// 删除：
commands::tcl::upload_to_tcl,
commands::tcl::check_tcl_available,
```

### 1.7 测试步骤

完成以上所有修改后：

```bash
# 1. 测试前端编译
npm run build

# 2. 测试 Tauri 编译
npm run tauri build

# 3. 运行开发模式验证
npm run tauri dev
```

**预期结果：**
- ✅ 编译无错误
- ✅ 设置页面中看不到 TCL 图床选项
- ✅ 上传页面中看不到 TCL 图床选项
- ✅ 历史记录筛选中看不到 TCL 选项

---

## 阶段二：修改京东图床 URL

### 2.1 修改 Rust 后端

**文件：** `src-tauri/src/commands/jd.rs`

**位置：** 约第 227-238 行

```rust
// 修改前：
let image_url = upload_response.path
    .ok_or_else(|| AppError::upload("京东", "API 未返回图片链接"))?;

println!("[京东] 上传成功: {}", image_url);

// 修改后：
let raw_url = upload_response.path
    .ok_or_else(|| AppError::upload("京东", "API 未返回图片链接"))?;

// 替换域名：dd-static.jd.com/ddimgp -> img30.360buyimg.com/imgzone
let image_url = raw_url.replace(
    "dd-static.jd.com/ddimgp",
    "img30.360buyimg.com/imgzone"
);

println!("[京东] 上传成功（原始URL: {}）", raw_url);
println!("[京东] 转换后URL: {}", image_url);
```

### 2.2 测试步骤

```bash
# 1. 重新编译 Rust
npm run tauri build

# 2. 运行开发模式
npm run tauri dev

# 3. 测试上传一张图片到京东图床
# 4. 检查返回的 URL 是否为新域名格式
```

**预期结果：**
- ✅ 上传到京东成功
- ✅ 返回的 URL 包含 `img30.360buyimg.com/imgzone`
- ✅ 图片可以正常访问和显示

---

## 阶段三：添加各图床缩略图支持

### 3.1 缩略图规则总览

| 图床 | 缩略图规则 | 说明 |
|------|-----------|------|
| 微博 | `thumb150/{pid}.jpg` | 服务端 150x150 缩略图 |
| R2 | `wsrv.nl/?url={url}&w=75&h=75...` | 使用第三方图片代理 |
| 京东 | `/s76x76_jfs/` 前缀 | 在 /jfs/ 后添加尺寸前缀 |
| 知乎 | `_xs` 后缀 | 扩展名前添加 _xs |
| 七鱼 | `?imageView&thumbnail=50x0` | NOS 图片处理参数 |
| 纳米 | `?x-tos-process=image/resize,l_75...` | TOS 图片处理参数 |
| 牛客 | `?x-oss-process=image%2Fresize...` | OSS 图片处理参数 |

### 3.2 修改各上传器

#### 文件 1：`src/uploaders/weibo/WeiboUploader.ts`

**位置：** 约第 145-148 行

```typescript
// 修改前：
getThumbnailUrl(result: UploadResult): string {
  const pid = result.fileKey;
  return `https://tvax1.sinaimg.cn/thumbnail/${pid}.jpg`;
}

// 修改后：
getThumbnailUrl(result: UploadResult): string {
  const pid = result.fileKey;
  return `https://tvax1.sinaimg.cn/thumb150/${pid}.jpg`;
}
```

#### 文件 2：`src/uploaders/r2/R2Uploader.ts`

**位置：** 约第 165 行

添加 `getThumbnailUrl` 方法（如果不存在）：

```typescript
/**
 * 生成 R2 缩略图 URL
 * 使用 wsrv.nl 图片代理服务生成缩略图
 */
getThumbnailUrl(result: UploadResult): string {
  const encodedUrl = encodeURIComponent(result.url);
  return `https://wsrv.nl/?url=${encodedUrl}&w=75&h=75&fit=cover&a=center&q=75&output=webp`;
}
```

#### 文件 3：`src/uploaders/jd/JDUploader.ts`

**位置：** 约第 116-118 行

```typescript
/**
 * 生成京东缩略图 URL
 * 在 /jfs/ 后添加 s76x76_ 前缀
 */
getThumbnailUrl(result: UploadResult): string {
  return result.url.replace('/jfs/', '/s76x76_jfs/');
}
```

#### 文件 4：`src/uploaders/zhihu/ZhihuUploader.ts`

**位置：** 约第 110 行

```typescript
/**
 * 生成知乎缩略图 URL
 * 在扩展名前添加 _xs 后缀
 */
getThumbnailUrl(result: UploadResult): string {
  return result.url.replace(/\.(\w+)$/, '_xs.$1');
}
```

#### 文件 5：`src/uploaders/qiyu/QiyuUploader.ts`

**位置：** 约第 93 行

```typescript
/**
 * 生成七鱼缩略图 URL
 * 使用 NOS 图片处理参数
 */
getThumbnailUrl(result: UploadResult): string {
  return `${result.url}?imageView&thumbnail=50x0`;
}
```

#### 文件 6：`src/uploaders/nami/NamiUploader.ts`

**位置：** 约第 96 行

```typescript
/**
 * 生成纳米缩略图 URL
 * 使用火山引擎 TOS 图片处理参数
 */
getThumbnailUrl(result: UploadResult): string {
  return `${result.url}?x-tos-process=image/resize,l_75/quality,q_70/format,jpg`;
}
```

#### 文件 7：`src/uploaders/nowcoder/NowcoderUploader.ts`

**位置：** 约第 93 行

```typescript
/**
 * 生成牛客缩略图 URL
 * 使用阿里云 OSS 图片处理参数
 */
getThumbnailUrl(result: UploadResult): string {
  return `${result.url}?x-oss-process=image%2Fresize%2Cw_75%2Ch_75%2Cm_mfit%2Fformat%2Cpng`;
}
```

### 3.3 创建统一缩略图生成函数

**文件：** `src/composables/useThumbCache.ts`

在文件开头添加新的导出函数：

```typescript
/**
 * 根据图床类型生成缩略图 URL
 * 供上传队列等非 composable 场景使用
 */
export function generateThumbnailUrl(
  serviceId: string,
  url: string,
  fileKey?: string,
  config?: UserConfig
): string {
  let thumbUrl: string;

  switch (serviceId) {
    case 'weibo':
      // 微博：使用 thumb150 获取 150x150 缩略图
      if (fileKey) {
        thumbUrl = `https://tvax1.sinaimg.cn/thumb150/${fileKey}.jpg`;
        // 应用链接前缀（如果启用）
        if (config) {
          const activePrefix = getActivePrefix(config);
          if (activePrefix) {
            thumbUrl = `${activePrefix}${thumbUrl}`;
          }
        }
      } else {
        thumbUrl = url;
      }
      break;

    case 'r2':
      // R2：使用 wsrv.nl 图片代理
      thumbUrl = `https://wsrv.nl/?url=${encodeURIComponent(url)}&w=75&h=75&fit=cover&a=center&q=75&output=webp`;
      break;

    case 'jd':
      // 京东：在 /jfs/ 后添加 s76x76_ 前缀
      thumbUrl = url.replace('/jfs/', '/s76x76_jfs/');
      break;

    case 'zhihu':
      // 知乎：在扩展名前添加 _xs 后缀
      thumbUrl = url.replace(/\.(\w+)$/, '_xs.$1');
      break;

    case 'qiyu':
      // 七鱼：使用 NOS 图片处理参数
      thumbUrl = `${url}?imageView&thumbnail=50x0`;
      break;

    case 'nami':
      // 纳米：使用火山引擎 TOS 图片处理参数
      thumbUrl = `${url}?x-tos-process=image/resize,l_75/quality,q_70/format,jpg`;
      break;

    case 'nowcoder':
      // 牛客：使用阿里云 OSS 图片处理参数
      thumbUrl = `${url}?x-oss-process=image%2Fresize%2Cw_75%2Ch_75%2Cm_mfit%2Fformat%2Cpng`;
      break;

    default:
      // 其他图床：直接使用原图
      thumbUrl = url;
  }

  return thumbUrl;
}
```

### 3.4 修改上传队列缩略图逻辑

**文件：** `src/components/UploadQueue.vue`

**位置：** 约第 220-224 行

```typescript
// 修改前（只处理微博）：
if (updatedItem.weiboPid && !updatedItem.thumbUrl) {
  const baiduPrefix = 'https://image.baidu.com/search/down?thumburl=';
  const bmiddleUrl = `https://tvax1.sinaimg.cn/bmiddle/${updatedItem.weiboPid}.jpg`;
  queueItems.value[index].thumbUrl = `${baiduPrefix}${bmiddleUrl}`;
}

// 修改后（支持所有图床）：
// 在文件顶部添加导入
import { generateThumbnailUrl } from '../composables/useThumbCache';
import { useConfigManager } from '../composables/useConfig';

// 在 setup 中获取 config
const { config } = useConfigManager();

// 修改缩略图生成逻辑
if (!updatedItem.thumbUrl && updatedItem.primaryUrl) {
  // 找到第一个上传成功的图床
  const successService = updatedItem.enabledServices.find(s => {
    const progress = updatedItem.serviceProgress[s];
    return progress?.status === 'success' && progress?.link;
  });

  if (successService) {
    const progress = updatedItem.serviceProgress[successService];
    if (progress?.link) {
      // 获取 fileKey（微博需要）
      const fileKey = successService === 'weibo' ? updatedItem.weiboPid : undefined;

      // 使用统一函数生成缩略图
      queueItems.value[index].thumbUrl = generateThumbnailUrl(
        successService,
        progress.link,
        fileKey,
        config.value
      );
    }
  }
}
```

### 3.5 测试步骤

```bash
# 1. 编译
npm run build

# 2. 运行
npm run tauri dev

# 3. 测试每个图床的缩略图
```

**测试清单：**

1. **微博图床**
   - [ ] 上传图片后，历史记录显示 150x150 缩略图
   - [ ] 缩略图 URL 包含 `thumb150`

2. **R2 图床**
   - [ ] 缩略图通过 wsrv.nl 代理加载
   - [ ] 尺寸为 75x75

3. **京东图床**
   - [ ] 缩略图 URL 包含 `/s76x76_jfs/`

4. **知乎图床**
   - [ ] 缩略图 URL 在扩展名前有 `_xs`

5. **七鱼图床**
   - [ ] 缩略图 URL 包含 `?imageView&thumbnail=50x0`

6. **纳米图床**
   - [ ] 缩略图 URL 包含 TOS 处理参数

7. **牛客图床**
   - [ ] 缩略图 URL 包含 OSS 处理参数

---

## 阶段四：新增美团图床

### 4.1 创建 Rust 后端

#### 文件：`src-tauri/src/commands/meituan.rs`

创建新文件并添加以下内容：

```rust
// src-tauri/src/commands/meituan.rs
// 美团图床上传命令

use tauri::{Window, Emitter};
use serde::{Deserialize, Serialize};
use reqwest::multipart;
use md5::{Md5, Digest};

use crate::error::{AppError, IntoAppError};
use super::utils::read_file_bytes;

/// 美团上传结果
#[derive(Debug, Serialize, Deserialize)]
pub struct MeituanUploadResult {
    pub url: String,
    pub size: u64,
}

/// 美团上传 API 响应
#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
struct MeituanUploadResponse {
    is_success: bool,
    url: Option<String>,
}

/// 文件大小限制：10MB
const MAX_FILE_SIZE: u64 = 10 * 1024 * 1024;

/// 生成随机 MD5 文件名
fn generate_random_filename(ext: &str) -> String {
    let timestamp = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .unwrap_or_default()
        .as_nanos();
    let random_str = format!("{}_{}", timestamp, rand::random::<u64>());
    let mut hasher = Md5::new();
    hasher.update(random_str.as_bytes());
    let hash = hasher.finalize();
    format!("{:x}.{}", hash, ext)
}

#[tauri::command]
pub async fn upload_to_meituan(
    window: Window,
    id: String,
    file_path: String,
) -> Result<MeituanUploadResult, AppError> {
    println!("[Meituan] 开始上传文件: {}", file_path);

    // 发送进度: 0% - 读取文件
    let _ = window.emit("upload://progress", serde_json::json!({
        "id": id,
        "progress": 0,
        "total": 100,
        "step": "读取文件...",
        "step_index": 1,
        "total_steps": 3
    }));

    // 1. 读取文件
    let (buffer, file_size) = read_file_bytes(&file_path).await?;

    // 2. 验证文件大小（限制 10MB）
    if file_size > MAX_FILE_SIZE {
        return Err(AppError::validation(format!(
            "文件大小 ({:.2}MB) 超过限制 (10MB)",
            file_size as f64 / 1024.0 / 1024.0
        )));
    }

    // 3. 获取文件扩展名
    let file_name = std::path::Path::new(&file_path)
        .file_name()
        .and_then(|n| n.to_str())
        .ok_or_else(|| AppError::validation("无法获取文件名"))?;

    let ext = file_name.split('.').last()
        .ok_or_else(|| AppError::validation("无法获取文件扩展名"))?
        .to_lowercase();

    if !["jpg", "jpeg", "png", "gif", "webp"].contains(&ext.as_str()) {
        return Err(AppError::validation("只支持 JPG、PNG、GIF、WebP 格式的图片"));
    }

    // 发送进度: 30% - 正在上传
    let _ = window.emit("upload://progress", serde_json::json!({
        "id": id,
        "progress": 30,
        "total": 100,
        "step": "正在上传...",
        "step_index": 2,
        "total_steps": 3
    }));

    // 4. 生成随机文件名
    let random_filename = generate_random_filename(&ext);

    // 5. 构建 multipart form
    let mime_type = match ext.as_str() {
        "jpg" | "jpeg" => "image/jpeg",
        "png" => "image/png",
        "gif" => "image/gif",
        "webp" => "image/webp",
        _ => "image/*"
    };

    let part = multipart::Part::bytes(buffer)
        .file_name(random_filename.clone())
        .mime_str(mime_type)
        .into_validation_err_with("无法设置 MIME 类型")?;

    let form = multipart::Form::new()
        .part("file", part)
        .text("name", random_filename);

    // 6. 发送请求到美团上传 API
    let client = reqwest::Client::new();
    let response = client
        .post("https://trust.dianping.com/upload.action")
        .header("Accept", "application/json, text/plain, */*")
        .header("User-Agent", "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36")
        .header("Origin", "https://trust.dianping.com")
        .header("Referer", "https://trust.dianping.com/")
        .multipart(form)
        .timeout(std::time::Duration::from_secs(60))
        .send()
        .await
        .into_network_err_with("上传请求失败")?;

    // 发送进度: 70% - 处理响应
    let _ = window.emit("upload://progress", serde_json::json!({
        "id": id,
        "progress": 70,
        "total": 100,
        "step": "处理响应...",
        "step_index": 3,
        "total_steps": 3
    }));

    // 7. 解析响应
    let response_text = response.text().await
        .into_network_err_with("无法读取响应")?;

    println!("[Meituan] 上传 API 响应: {}", response_text);

    let upload_response: MeituanUploadResponse = serde_json::from_str(&response_text)
        .map_err(|e| AppError::upload("美团", format!("JSON 解析失败: {}", e)))?;

    // 8. 检查上传结果
    if !upload_response.is_success {
        return Err(AppError::upload("美团", "上传失败，API 返回 isSuccess=false"));
    }

    let raw_url = upload_response.url
        .ok_or_else(|| AppError::upload("美团", "API 未返回图片链接"))?;

    // 9. URL 处理：http -> https，p0.meituan.net -> img.meituan.net
    let image_url = raw_url
        .replace("http://", "https://")
        .replace("p0.meituan.net", "img.meituan.net");

    println!("[Meituan] 上传成功: {}", image_url);

    Ok(MeituanUploadResult {
        url: image_url,
        size: file_size,
    })
}
```

#### 修改：`src-tauri/src/commands/mod.rs`

```rust
pub mod upload;
pub mod r2;
pub mod jd;
pub mod meituan;  // ← 添加这行
pub mod nowcoder;
// ... 其他模块
```

#### 修改：`src-tauri/src/main.rs`

在 `invoke_handler` 中添加命令：

```rust
.invoke_handler(tauri::generate_handler![
    // ... 其他命令
    commands::meituan::upload_to_meituan,  // ← 添加这行
    // ... 其他命令
])
```

### 4.2 创建前端上传器

#### 创建目录结构

```
src/uploaders/meituan/
  ├── MeituanUploader.ts
  ├── MeituanError.ts
  └── index.ts
```

#### 文件 1：`src/uploaders/meituan/MeituanUploader.ts`

```typescript
// src/uploaders/meituan/MeituanUploader.ts
// 美团图床上传器实现

import { BaseUploader } from '../base/BaseUploader';
import { UploadResult, ValidationResult, UploadOptions, ProgressCallback } from '../base/types';
import { convertToMeituanError } from './MeituanError';

/**
 * Rust 返回的美团上传结果
 */
interface MeituanRustResult {
  url: string;
  size: number;
}

/**
 * 美团图床上传器
 * 美团图床无需认证，完全开箱即用
 */
export class MeituanUploader extends BaseUploader {
  readonly serviceId = 'meituan';
  readonly serviceName = '美团图床';

  /**
   * 返回对应的 Rust 命令名
   */
  protected getRustCommand(): string {
    return 'upload_to_meituan';
  }

  /**
   * 验证美团配置
   * 美团图床无需配置，直接返回 valid
   */
  async validateConfig(_config: any): Promise<ValidationResult> {
    return { valid: true };
  }

  /**
   * 上传文件到美团
   */
  async upload(
    filePath: string,
    _options: UploadOptions,
    onProgress?: ProgressCallback
  ): Promise<UploadResult> {
    this.log('info', '开始上传到美团', { filePath });

    try {
      const rustResult = await this.uploadViaRust(
        filePath,
        {},
        onProgress
      ) as MeituanRustResult;

      this.log('info', '美团上传成功', { url: rustResult.url });

      return {
        serviceId: 'meituan',
        fileKey: rustResult.url,
        url: rustResult.url,
        size: rustResult.size
      };
    } catch (error: any) {
      this.log('error', '美团上传失败', error);
      throw convertToMeituanError(error);
    }
  }

  /**
   * 生成美团公开访问 URL
   */
  getPublicUrl(result: UploadResult): string {
    return result.url;
  }

  /**
   * 生成美团缩略图 URL
   * 美团缩略图：添加 @75w_75h_1e_1c_60Q.webp 后缀
   */
  getThumbnailUrl(result: UploadResult): string {
    return `${result.url}@75w_75h_1e_1c_60Q.webp`;
  }

  /**
   * 生成美团原图 URL
   */
  getOriginalUrl(result: UploadResult): string {
    return result.url;
  }
}
```

#### 文件 2：`src/uploaders/meituan/MeituanError.ts`

```typescript
// src/uploaders/meituan/MeituanError.ts
// 美团图床错误处理

import { UploadErrorCode, StructuredError, createStructuredError } from '../base/ErrorTypes';

/**
 * 美团上传错误类
 */
export class MeituanUploadError extends Error {
  constructor(
    message: string,
    public readonly code?: UploadErrorCode,
    public readonly originalError?: any
  ) {
    super(message);
    this.name = 'MeituanUploadError';
  }
}

/**
 * 转换通用错误为美团结构化错误
 */
export function convertToMeituanError(error: any): StructuredError {
  const msg = error?.message || String(error);
  const lowerMsg = msg.toLowerCase();

  // 网络错误
  if (lowerMsg.includes('network') || lowerMsg.includes('timeout') || lowerMsg.includes('econnrefused')) {
    return createStructuredError(
      UploadErrorCode.NETWORK_ERROR,
      '美团网络错误：无法连接到美团图床服务',
      {
        details: msg,
        retryable: true,
        solution: '请检查网络连接后重试',
        originalError: error,
        serviceId: 'meituan'
      }
    );
  }

  // 限流错误
  if (lowerMsg.includes('rate limit') || lowerMsg.includes('too many requests') || lowerMsg.includes('429')) {
    return createStructuredError(
      UploadErrorCode.RATE_LIMIT,
      '美团限流错误：请求过于频繁',
      {
        details: msg,
        retryable: true,
        solution: '请稍后再试，避免频繁上传',
        originalError: error,
        serviceId: 'meituan'
      }
    );
  }

  // 文件错误
  if (lowerMsg.includes('file') && (lowerMsg.includes('not found') || lowerMsg.includes('无法找到'))) {
    return createStructuredError(
      UploadErrorCode.FILE_NOT_FOUND,
      '美团文件错误：文件不存在或无法读取',
      {
        details: msg,
        retryable: false,
        solution: '请确认文件路径正确',
        originalError: error,
        serviceId: 'meituan'
      }
    );
  }

  // 文件过大
  if (lowerMsg.includes('too large') || lowerMsg.includes('size') || lowerMsg.includes('过大')) {
    return createStructuredError(
      UploadErrorCode.FILE_TOO_LARGE,
      '美团文件错误：文件过大，超出限制',
      {
        details: msg,
        retryable: false,
        solution: '请压缩图片后重试',
        originalError: error,
        serviceId: 'meituan'
      }
    );
  }

  // 服务器错误
  if (lowerMsg.includes('500') || lowerMsg.includes('502') || lowerMsg.includes('503')) {
    return createStructuredError(
      UploadErrorCode.SERVER_ERROR,
      '美团服务器错误：服务暂时不可用',
      {
        details: msg,
        retryable: true,
        solution: '请稍后重试',
        originalError: error,
        serviceId: 'meituan'
      }
    );
  }

  // 通用上传错误
  return createStructuredError(
    UploadErrorCode.UPLOAD_FAILED,
    `美团上传失败: ${msg}`,
    {
      details: msg,
      retryable: true,
      originalError: error,
      serviceId: 'meituan'
    }
  );
}
```

#### 文件 3：`src/uploaders/meituan/index.ts`

```typescript
// src/uploaders/meituan/index.ts
export { MeituanUploader } from './MeituanUploader';
```

### 4.3 修改类型定义

**文件：** `src/config/types.ts`

#### 操作 1：添加到 ServiceType

```typescript
// 修改前：
export type ServiceType = 'weibo' | 'r2' | 'jd' | 'nowcoder' | 'qiyu' | 'zhihu' | 'nami';

// 修改后：
export type ServiceType = 'weibo' | 'r2' | 'meituan' | 'jd' | 'nowcoder' | 'qiyu' | 'zhihu' | 'nami';
```

#### 操作 2：添加到 PUBLIC_SERVICES

```typescript
// 修改前：
export const PUBLIC_SERVICES: readonly ServiceType[] = ['weibo', 'jd', 'nowcoder', 'qiyu', 'zhihu', 'nami'] as const;

// 修改后：
export const PUBLIC_SERVICES: readonly ServiceType[] = ['weibo', 'meituan', 'jd', 'nowcoder', 'qiyu', 'zhihu', 'nami'] as const;
```

#### 操作 3：添加 MeituanServiceConfig 接口

```typescript
/**
 * 美团图床配置
 */
export interface MeituanServiceConfig {
  enabled: boolean;
}
```

#### 操作 4：在 UserConfig.services 中添加字段

```typescript
export interface UserConfig {
  // ...
  services: {
    weibo: WeiboServiceConfig;
    r2: R2ServiceConfig;
    meituan: MeituanServiceConfig;  // ← 添加这行
    jd: JDServiceConfig;
    // ...
  };
}
```

#### 操作 5：在 DEFAULT_CONFIG 中添加默认配置

```typescript
export const DEFAULT_CONFIG: UserConfig = {
  // ...
  services: {
    weibo: { /* ... */ },
    r2: { /* ... */ },
    meituan: {           // ← 添加这个配置块
      enabled: true
    },
    jd: { /* ... */ },
    // ...
  }
};
```

### 4.4 注册上传器

**文件：** `src/uploaders/index.ts`

#### 操作 1：添加导入

```typescript
import { MeituanUploader } from './meituan/MeituanUploader';
```

#### 操作 2：注册上传器

```typescript
export function initializeUploaders(): void {
  console.log('[Uploaders] 开始注册上传器...');

  // ... 其他上传器

  // 注册美团上传器
  UploaderFactory.register('meituan', () => new MeituanUploader());

  // ... 其他上传器
}
```

#### 操作 3：导出上传器

```typescript
export { MeituanUploader } from './meituan';
```

### 4.5 修改其他组件

#### 文件 1：`src/core/MultiServiceUploader.ts`

添加美团错误转换：

```typescript
// 导入
import { convertToMeituanError } from '../uploaders/meituan/MeituanError';

// 在 switch 语句中添加
case 'meituan':
  structuredError = convertToMeituanError(error);
  break;

// 在 filterConfiguredServices 中添加
if (service === 'meituan') return true; // 美团图床无需配置
```

#### 文件 2：`src/composables/useUpload.ts`

```typescript
const serviceConfigStatus = ref({
  // ...
  meituan: true,  // ← 添加这行
  // ...
});
```

#### 文件 3：`src/components/views/UploadView.vue`

```typescript
// serviceLabels
const serviceLabels: Record<ServiceType, string> = {
  // ...
  meituan: '美团',  // ← 添加这行
  // ...
};

// allServices
const allServices: ServiceType[] = ['weibo', 'r2', 'meituan', 'jd', ...];
```

#### 文件 4：`src/components/views/SettingsView.vue`

```typescript
// availableServices
const availableServices: ServiceType[] = ['weibo', 'r2', 'meituan', 'jd', ...];

// serviceLabels
const serviceLabels: Record<ServiceType, string> = {
  // ...
  meituan: '美团',
  // ...
};

// isOutOfBoxService
function isOutOfBoxService(service: ServiceType): boolean {
  return ['meituan', 'jd', 'nowcoder', 'qiyu', 'zhihu', 'nami'].includes(service);
}
```

可选：添加美团图床 UI 面板（位置约第 1900 行，在 R2 面板之后）：

```vue
<!-- 美团图床 -->
<div v-if="activeTab === 'meituan'" class="service-config-panel">
  <div class="config-header">
    <h3>美团图床</h3>
    <div class="config-status out-of-box">
      <i class="pi pi-check-circle"></i>
      <span>开箱即用</span>
    </div>
  </div>

  <div class="config-section">
    <div class="info-box">
      <i class="pi pi-info-circle"></i>
      <div>
        <p><strong>美团图床无需配置，启用后即可使用</strong></p>
        <ul>
          <li>支持格式：JPG、PNG、GIF、WebP</li>
          <li>文件大小：最大 10MB</li>
          <li>访问速度：国内访问快速</li>
          <li>图片稳定性：依赖美团服务稳定性</li>
        </ul>
      </div>
    </div>
  </div>

  <div class="config-actions">
    <Button
      :label="config.services.meituan.enabled ? '禁用美团图床' : '启用美团图床'"
      :severity="config.services.meituan.enabled ? 'danger' : 'success'"
      @click="config.services.meituan.enabled = !config.services.meituan.enabled"
    />
  </div>
</div>
```

#### 文件 5：`src/components/views/history/HistoryTableView.vue`

```typescript
const serviceLabels: Record<ServiceType, string> = {
  // ...
  meituan: '美团',
  // ...
};
```

#### 文件 6：`src/components/views/history/HistoryLightbox.vue`

```typescript
const serviceLabels: Record<ServiceType, string> = {
  // ...
  meituan: '美团',
  // ...
};
```

#### 文件 7：`src/components/views/HistoryView.vue`

```typescript
const serviceOptions = [
  // ...
  { label: '美团', value: 'meituan' },
  // ...
];
```

#### 文件 8：`src/components/views/LinkCheckerView.vue`

```typescript
const serviceOptions = [
  // ...
  { label: '美团图床', value: 'meituan' },
  // ...
];
```

#### 文件 9：`src/services/RetryService.ts`

在所有 `serviceLabels` 定义处添加：

```typescript
const serviceLabels: Record<ServiceType, string> = {
  // ...
  meituan: '美团',
  // ...
};
```

#### 文件 10：`src/components/UploadQueue.vue`

```typescript
// serviceNames
const serviceNames: Record<ServiceType, string> = {
  // ...
  meituan: '美团',
  // ...
};

// serviceIcons（可选）
const serviceIcons: Record<ServiceType, string> = {
  // ...
  meituan: 'pi-cloud-upload',  // 或其他合适的图标
  // ...
};
```

#### 文件 11：`src/composables/useThumbCache.ts`

在 `generateThumbnailUrl` 函数的 switch 语句中添加：

```typescript
case 'meituan':
  // 美团：添加缩略图后缀
  thumbUrl = `${url}@75w_75h_1e_1c_60Q.webp`;
  break;
```

### 4.6 测试步骤

```bash
# 1. 编译 Rust
npm run tauri build

# 2. 运行开发模式
npm run tauri dev
```

**测试清单：**

1. **基本功能**
   - [ ] 设置页面能看到美团图床选项
   - [ ] 美团图床默认启用
   - [ ] 美团图床显示"开箱即用"标识

2. **上传功能**
   - [ ] 能成功上传图片到美团
   - [ ] 返回的 URL 是 HTTPS 协议
   - [ ] 返回的 URL 包含 `img.meituan.net`
   - [ ] 上传的图片可以正常访问

3. **缩略图功能**
   - [ ] 历史记录中显示美团缩略图
   - [ ] 缩略图 URL 包含 `@75w_75h_1e_1c_60Q.webp`

4. **文件限制**
   - [ ] 上传 > 10MB 的文件会报错
   - [ ] 上传非图片文件会报错
   - [ ] 支持的格式（JPG、PNG、GIF、WebP）都能上传

5. **错误处理**
   - [ ] 网络错误时有友好提示
   - [ ] 上传失败时显示正确的错误信息

---

## 测试清单

### 编译测试

```bash
# 前端编译
npm run build

# Tauri 编译
npm run tauri build

# 开发模式
npm run tauri dev
```

**预期结果：**
- [ ] 前端编译无错误无警告
- [ ] Rust 编译无错误无警告
- [ ] 应用能正常启动

### 功能测试

#### 1. TCL 删除验证

- [ ] 设置页面看不到 TCL 选项
- [ ] 上传页面看不到 TCL 选项
- [ ] 历史记录筛选看不到 TCL
- [ ] 链接检查器看不到 TCL

#### 2. 京东 URL 验证

- [ ] 上传图片到京东
- [ ] 返回的 URL 包含 `img30.360buyimg.com/imgzone`
- [ ] 图片可以正常访问

#### 3. 缩略图验证

对每个图床测试：
- [ ] 微博缩略图（thumb150）
- [ ] R2 缩略图（wsrv.nl）
- [ ] 京东缩略图（s76x76_jfs）
- [ ] 知乎缩略图（_xs）
- [ ] 七鱼缩略图（NOS 参数）
- [ ] 纳米缩略图（TOS 参数）
- [ ] 牛客缩略图（OSS 参数）
- [ ] 美团缩略图（@后缀）

#### 4. 美团图床验证

- [ ] 能在设置中看到美团选项
- [ ] 能成功上传到美团
- [ ] URL 格式正确（HTTPS + img.meituan.net）
- [ ] 图片可以访问
- [ ] 缩略图正常显示
- [ ] 文件大小限制生效
- [ ] 格式限制生效

### 回归测试

- [ ] 上传到微博正常
- [ ] 上传到 R2 正常
- [ ] 上传到京东正常
- [ ] 上传到其他图床正常
- [ ] 多图床并行上传正常
- [ ] 历史记录正常显示
- [ ] 链接检查功能正常

---

## 常见问题

### Q1: 编译时提示找不到 TCL 模块

**原因：** 删除不完整，还有地方引用了 TCL

**解决：** 搜索项目中所有包含 `tcl` 的代码（不区分大小写），确保完全删除

```bash
# 搜索前端代码
grep -ri "tcl" src/

# 搜索 Rust 代码
grep -ri "tcl" src-tauri/
```

### Q2: 美团上传失败

**可能原因：**
1. 网络问题
2. 文件过大（>10MB）
3. 文件格式不支持

**排查步骤：**
1. 检查控制台错误信息
2. 查看 Rust 后台日志
3. 确认文件大小和格式

### Q3: 缩略图不显示

**可能原因：**
1. 缩略图 URL 生成错误
2. 图床不支持该缩略图参数
3. 网络加载失败

**排查步骤：**
1. 打开浏览器开发者工具查看网络请求
2. 复制缩略图 URL 在浏览器中直接访问
3. 检查 `generateThumbnailUrl` 函数逻辑

### Q4: TypeScript 编译错误

**常见错误：**
- `Property 'tcl' does not exist`：还有地方使用了 TCL
- `Type 'meituan' is not assignable`：类型定义不完整

**解决：**
1. 确保 `types.ts` 中的类型定义正确
2. 确保所有使用 `ServiceType` 的地方都更新了

---

## 开发建议

### 1. 分步提交

建议每完成一个阶段就提交一次 Git：

```bash
# 阶段一完成后
git add .
git commit -m "删除 TCL 图床"

# 阶段二完成后
git commit -m "修改京东图床 URL"

# 阶段三完成后
git commit -m "添加各图床缩略图支持"

# 阶段四完成后
git commit -m "新增美团图床"
```

### 2. 分步测试

每完成一个阶段立即测试，不要等全部完成再测试。

### 3. 备份配置

修改前备份用户配置文件：

```
%APPDATA%\us.picnex.app\config.json
```

### 4. 日志调试

遇到问题时查看日志：
- 前端日志：浏览器控制台
- Rust 日志：终端输出（开发模式）

---

## 参考资料

### API 文档

- **美团上传 API**：`https://trust.dianping.com/upload.action`
  - 请求方式：POST multipart/form-data
  - 字段：`file`（文件）、`name`（文件名）
  - 响应：`{ isSuccess: boolean, url: string }`

### 缩略图参数

- **微博**：`https://tvax1.sinaimg.cn/thumb150/{pid}.jpg`
- **wsrv.nl**：`https://wsrv.nl/?url={url}&w=75&h=75&fit=cover&a=center&q=75&output=webp`
- **京东**：在 `/jfs/` 后添加 `s76x76_` 前缀
- **知乎**：扩展名前添加 `_xs`
- **七鱼**：`?imageView&thumbnail=50x0`
- **纳米**：`?x-tos-process=image/resize,l_75/quality,q_70/format,jpg`
- **牛客**：`?x-oss-process=image%2Fresize%2Cw_75%2Ch_75%2Cm_mfit%2Fformat%2Cpng`
- **美团**：`@75w_75h_1e_1c_60Q.webp`

---

## 结语

本文档提供了详细的分步开发指南，建议按照阶段顺序逐步实施。每个阶段都可以独立开发和测试，降低了出错风险。

如果遇到问题，请：
1. 查看本文档的"常见问题"部分
2. 检查终端和浏览器控制台的错误信息
3. 使用 Git 回退到上一个稳定状态

祝开发顺利！
