#!/usr/bin/env sh
# --concurrent 1 的原因：
# Windows 有 8191 字符的命令行长度上限，lint-staged 会据此把暂存文件切成多个
# chunk。默认并发执行意味着每个 chunk 各起一个 `vitest related --run`，在
# 200+ 文件的重构提交里会同时跑起 6 个 vitest 进程互相抢内存，最终被系统
# SIGKILL —— 表现为"测试失败"，实际是资源耗尽的假失败。
# 串行慢一些，但小改动（只有一个 chunk）没有任何区别。
npx lint-staged --concurrent 1
