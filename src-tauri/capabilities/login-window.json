{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "login-window",
  "description": "Login parent window capability",
  "windows": ["login-window"],
  "permissions": [
    "core:window:allow-close"
  ]
}
